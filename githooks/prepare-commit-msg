#!/bin/sh
# Strip Co-authored-by lines so only the primary author appears in commits.
# Install: cp githooks/prepare-commit-msg .git/hooks/prepare-commit-msg && chmod +x .git/hooks/prepare-commit-msg
if [ -f "$1" ]; then
  grep -v '^Co-authored-by:' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
fi
